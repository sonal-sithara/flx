@page("/user/:id")
composable UserScreen(id) {
  val nav = useNavigator()

  Column(padding: 16, gap: 12, main: .center) {
    Text("User profile", style: .title)
    Text("User id: ${id}")
    Text("Opened via route /user/${id}", style: .caption)
    Button("Back") {
      nav.pop()
    }
  }
}
