# Local development only — never published, and ignored by consumers.
# Keeps flx_lsp building against the compiler in this checkout rather than the
# last release.
dependency_overrides:
  flx_compiler:
    path: ../flx_compiler
